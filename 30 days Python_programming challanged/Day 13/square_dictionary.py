# Generate the dictionary where keys are numbers between 1 and 20 and values are squares of keys
square_dict = {num: num ** 2 for num in range(1, 21)}

# Print the dictionary
print("Dictionary where keys are numbers between 1 and 20 and values are squares of keys:")
print(square_dict)
